// header 组件
// header 的模版
// import render from './tpl';
import './index.less';

// 获取 index.html 里的位置
// const wrapper = document.getElementById('header');

// 把从 ajax 获取的数据，渲染到页面上面
// 给 render 函数传递一个 json
// 然后在模版里 tpl.js 里，可以引用 json 的值
// wrapper.innerHTML = render({
//   text: 'haha',
//   a: 1,
//   b: 2,
//   c: 3,
// });

// todo
